#!/bin/sh

exec Terminal --working-directory=$(xdg-user-dir DESKTOP) \
	--icon=email-client --name=mail \
	"--title=Client de courrier électronique - Neomutt" \
	-- /usr/bin/neomutt "$@"


# urxvtc -cd $(xdg-user-dir DESKTOP) -name mail -title mutt -e /usr/bin/neomutt "$@"

# vim: set ft=sh ai ts=4 sw=4 tw=79:
