setopt no_global_rcs
export skip_global_compinit=1
export DEFAULT_USER="canalguada"
export ZSH_CACHE_DIR="${XDG_CACHE_HOME:-$HOME/.cache}/zsh"
export ZDOTDIR="$HOME/.zsh"
export GPG_TTY=$(tty)

# [[ -r "$HOME/.shinit" ]] && source "$HOME/.shinit"

[[ -r "$HOME/.local/lib/sh/aliases.lib.sh" ]] &&
	source "$HOME/.local/lib/sh/aliases.lib.sh"
[[ -r "$HOME/.local/lib/sh/exports.lib.sh" ]] &&
	source "$HOME/.local/lib/sh/exports.lib.sh"
